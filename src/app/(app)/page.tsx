// This page is intentionally left blank or can be used for redirection
// to prevent path conflicts if the (app) group root is accessed.
// Most likely, users should be directed to a specific page like /dashboard.
export default function AppRootPage() {
  // Optionally, you could redirect to dashboard here if this path is ever hit:
  // import { redirect } from 'next/navigation';
  // redirect('/dashboard');
  return null;
}
